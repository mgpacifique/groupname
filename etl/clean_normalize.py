def normalize_phone(phone_string):
    """Formats phone entries cleanly to a unified country code string."""
    # Logic to process phone structures clean
    return phone_string

def clean_amount(amount_string):
    """Extracts clean numeric balances/amounts from raw string bodies."""
    return float(amount_string.replace(",", ""))
